{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start": "php artisan serve --host=0.0.0.0 --port=8000",
        "render-build": "npm install && npm run build",
        "prod": "composer install --optimize-autoloader --no-dev && npm run build"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.8.2",
        "laravel-vite-plugin": "^1.2.0",
        "tailwindcss": "^4.0.0",
        "vite": "^6.2.4"
    },
    "dependencies": {
        "concurrently": "^9.0.1"
    }
}